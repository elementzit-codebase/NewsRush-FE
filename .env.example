# Copy to .env.local to override the committed defaults in .env.

# Where the dev server proxies /api and /uploads (config time only).
API_PROXY_TARGET=http://127.0.0.1:8000

# Runtime API prefix. Empty = same-origin, which uses the dev proxy.
VITE_API_BASE=
